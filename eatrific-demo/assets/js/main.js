const reveals=[...document.querySelectorAll('.reveal')];const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');observer.unobserve(e.target)}}),{threshold:.12});reveals.forEach(el=>observer.observe(el));const menuToggle=document.getElementById('menuToggle');const navLinks=document.getElementById('navLinks');if(menuToggle&&navLinks){menuToggle.addEventListener('click',()=>navLinks.classList.toggle('open'));navLinks.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>navLinks.classList.remove('open')))}const chips=[...document.querySelectorAll('.chip')];const categories=[...document.querySelectorAll('.menu-category')];const search=document.getElementById('menuSearch');function applyMenuFilter(){if(!categories.length)return;const active=document.querySelector('.chip.active')?.dataset.filter||'all';const q=(search?.value||'').toLowerCase().trim();categories.forEach(section=>{const cat=section.dataset.category;let any=false;section.querySelectorAll('.menu-item').forEach(item=>{const ok=!q||item.textContent.toLowerCase().includes(q);item.style.display=ok?'flex':'none';if(ok)any=true});section.classList.toggle('is-hidden',!(active==='all'||active===cat)||!any)})}chips.forEach(chip=>chip.addEventListener('click',()=>{chips.forEach(c=>c.classList.remove('active'));chip.classList.add('active');applyMenuFilter()}));search?.addEventListener('input',applyMenuFilter);